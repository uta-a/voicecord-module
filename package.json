{
  "name": "voicecord-module",
  "version": "0.1.0",
  "private": true,
  "description": "VoiceCord — Discord 内蔵クライアント mod（asar shim + patcher + preload UI + frida engine）",
  "author": "uta-a",
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@electron/asar": "^4.3.0",
    "@types/node": "^22.9.0",
    "typescript": "^5.6.3",
    "vitest": "^2.1.9"
  },
  "allowScripts": {
    "esbuild@0.21.5": true
  }
}

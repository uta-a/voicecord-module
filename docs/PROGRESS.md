# 進捗確認

確認日: 2026-09-15
確認時の HEAD: `957a3ad`

ソース、コミット履歴、TASKS.md、DEV-NOTES.md、配布成果物を確認した。
確認開始時の作業ツリーはクリーン。今回は実機操作とテスト再実行を行っていない。
実機結果は 2026-09-14 の記録に基づく。

| 段階 | 現在の状態 | 主な残件 |
| --- | --- | --- |
| M0 | 旧経路の基準線はチェック未完了 | 旧構成の記録。新 mod の検証で旧アプリを起動しない |
| M1 | shim・マネージャ・UI 基盤は実装済み | Discord 回帰の残り、24 時間常用、DevTools オプトイン |
| M2 | 完了 | IPC・設定移行・エンジン監督の検証記録あり |
| M3 | 完了。dispose 修正版の kill 後も受信側で即時停止を確認 | 閉鎖時間は手動受け入れ。厳密な数値測定は未実施 |
| M4 | 完了。デコード比較・WMA 表示・全音源調整・声の再校正を確認済み | 多チャンネル素材は検証対象外 |
| M4.5 | 採取役・接ぎ木・ポップアウト・テーマ追従を実装済み | ライト・ミッドナイト、画面移動、再挿入回数、Vencord 共存、FAB フォールバックの実機確認 |
| M5 | dispose とグローバル緊急停止を実装済み | VC 再生中の停止と受信側のゲート閉鎖、終了・復帰経路の実機確認 |
| M6 | portable exe の設定と成果物あり。AV 案内を実装済み | exe からの適用・解除・再適用・復旧の受け入れ、ドキュメント整合 |
| M7 | 未着手 | Canary の受け入れ後に Stable の適用範囲を確認 |

## 前回から進んだ実装

- `990e167` / `0b63d0c`: メインワールドの読み取り専用採取役と純正ボタン横への接ぎ木。
- `163f6b9` / `6facbac`: 純正意匠のポップアウトと Discord テーマ変数への追従。
- `f727514` / `fdfae60`: ボタンの縦積みとポップアウトの塗りを実機で修正。
- `9f79132` / `a92c9dd`: スクリプト解除時のゲート閉鎖と緊急停止 `Ctrl+Alt+Shift+X`。
- `7dfb1f4` / `3ea970d`: AV 除外案内と portable exe の配布設定。

M4.5 はサーバー VC で純正ボタンの隣への配置、FAB 非表示、ポップアウト表示を確認済み。Vencord 共存時の CSS 採取と受信音声も Canary 1.0.1173 で確認済み。未計測レートの 48 kHz フォールバックによる低速・低ピッチを Canary 32 kHz 補完で修正した。
緊急停止は登録成功と engine への到達を確認済み。受信側の停止確認はまだない。

`dist/VoiceCord-0.1.0-portable.exe` は存在する。サイズは 133841041 バイト。
更新時刻は 2026-09-14 20:50:11。成果物の存在だけで配布版の受け入れ完了とはしない。

## タスク記録の注意点

TASKS.md の M4.5-b〜g には実装済みの項目が未チェックのまま残っている。
「M4 完了後に着手」という文も履歴上の計画であり、現在は M4 の校正を残して UI 実装が進んでいる。
アンカー探索の実装は CSS モジュール、クラス名の共通部分、aria-label、構造の 4 段と FAB。
計画にあったチャット欄の `expression-picker-chat-input-button` は実測で不適合と判明したため使わない。

M6 の旧 README 訂正は TASKS.md に完了記録がある一方、下の節には未実施の文が残る。
旧解析文書を触らないユーザー決定もあるため、古い「M6 で訂正」の文は現在の変更範囲と区別する。
M3 の「hook.js を無改造で移植」は移植時の事実。現在は dispose 修正が入っている。
記録済みの 401 テスト成功は以前の検証であり、現在の全実装の最新実行結果とは断定しない。

## 次の順序

1. VC 中の緊急停止を受信側と検証する。dispose 修正後の kill は下の追記で確認済み。
2. 新しい左右平均を維持して音源別音量と声の校正を見直す。
3. M4.5 のテーマ・画面移動・再挿入・Vencord 共存・FAB の重なりを確認する。
4. M1 の回帰残件と 24 時間常用を完了する。
5. portable exe の適用・解除・更新後の再適用・復旧を受け入れる。
6. Canary で必要な確認を終えてから M7 へ進む。

検証条件と数値は [DEV-NOTES.md](DEV-NOTES.md)、詳細な履歴は [TASKS.md](TASKS.md) を参照する。

## 2026-09-15 の実機追記

Canary 1.0.1169。配置済み hook とソースの SHA-256 が一致することを確認。
一般 VC で受信側が chime を聞いている間に engine PID 13696 を強制終了した。
kill は 2026-09-14 19:35:09.517 UTC。受信側は音・送信インジケーターの消失を「すぐ」と報告。
2 秒以内の条件は手動受け入れとして確認済み。正確なミリ秒値は測定していない。
19:35:17.442 UTC の確認時点で engine PID 42140 が renderer PID 47720 に再アタッチ済み。
注入レートは 32000 Hz / 320 サンプル。緊急停止の登録失敗を示す degraded は 0 件。

復帰後の chime を再生してユーザーに Ctrl+Alt+Shift+X を押してもらった。
受信側は停止までの時間を「すぐ」と報告。VC 中の緊急停止は手動受け入れで確認済み。
その後も engine PID 42140 / renderer PID 47720 のアタッチと 32000 Hz を維持。
復帰後のピッチ・速度について今回の回答はなく、今回の確認は停止動作を対象とする。
M5 全体の終了・復帰経路の確認は引き続き残る。

続いて実際の preload API の sourceStats で 32 音源を読み取り測定。32 件すべて成功。
設定更新を伴うダイアログの測定ボタンは押していない。個別音量・基準値・校正値の変更はなし。
既存の個別音量は 32 件。全件への推奨値適用は既存調整も上書きするため対象の選択待ち。

ユーザーが全 32 音源への推奨値適用を選択したため、音量調整ダイアログで測定・全件適用した。
28 件の値が変わり、残り 4 件は元から推奨値の上限 150% と一致。
適用後に UI の「すべて推奨値どおりです」と getConfig の保存値を確認。
基準は -16.3 から -16.4 dBFS。master は 1.0007477687304072 のまま。
声の校正値と目標 -6.5 dB はまだ旧測定のまま。声の再測定と受信側のバランス確認は残る。

個別音量の適用前後。表示はパーセントに丸めた値。

| 音源 | 前 | 後 |
| --- | ---: | ---: |
| 1512880262558519326 | 71 | 98 |
| bye-bye-soundbible | 92 | 109 |
| chime | 150 | 150 |
| co | 67 | 90 |
| da-na-yan-azuki_2CDzUji | 66 | 83 |
| deja-vu-fade | 66 | 91 |
| kai-shi-dana | 94 | 132 |
| meme-de-creditos-finales_qHtIjyQ | 86 | 122 |
| musuka-ren-gomi | 52 | 66 |
| ofeng-lu-gafei-kimashita | 47 | 63 |
| okawariitadaketadarouka | 107 | 150 |
| panpanpanpanpanpan-panpan_kasekMB | 74 | 87 |
| pokemon-hui-fu-yin | 117 | 135 |
| ren-noxin-tokanainka_JVPTSaf | 66 | 82 |
| roblox-death-sound_1 | 97 | 119 |
| suzo-susuru | 72 | 89 |
| voice-message | 112 | 97 |
| voice-message 1 | 68 | 59 |
| voice-message 2 | 76 | 62 |
| voice-message 3 | 102 | 83 |
| voice-message 4 | 90 | 82 |
| waho-seikin | 120 | 150 |
| yarimasune | 113 | 130 |
| zeruda-mi-jie-kiyin-burewaiver | 150 | 150 |
| しゅわしゅわハニーレモン0ml | 118 | 150 |
| ねこのは | 97 | 119 |
| ねこのは２ | 90 | 96 |
| ねこのは３ | 95 | 134 |
| みいちゃん | 109 | 103 |
| 湘南キャンパス-チャイム音 - 東海大学公式 | 80 | 98 |
| ｺﾝﾆﾁﾜ | 150 | 150 |
| ﾍﾙﾑｰﾃｩ | 150 | 150 |

150% 上限は chime、okawariitadaketadarouka、waho-seikin、zeruda-mi-jie-kiyin-burewaiver、
しゅわしゅわハニーレモン0ml、ｺﾝﾆﾁﾜ、ﾍﾙﾑｰﾃｩ の 7 件。
ピーク制約は meme-de-creditos-finales_qHtIjyQ と ねこのは３ の 2 件。
この 9 件は基準より小さくなる。左右平均の方式は変更していない。

## UI 動作修正の引き継ぎ

2026-09-15 のユーザー指示: 純正サウンドボードのボタン動作に忠実に合わせる。
再生中の行は不要。不要なレイアウト変更を行わない。
SoundboardPopout.tsx の条件付き NowPlaying 行を削除。タイルとフッターの動作はまだ変更していない。
純正の再生クリック・ホバー・連続クリック・停止表示の実機比較と Canary 反映は未完了。

Playwright MCP 再起動後に --headless 起動と Canary CDP 接続を確認。
純正 airhorn のタイルをホバー・クリックし、再生後も 531x520 のポップアウトが開いたままで
中央が再生アイコンのままであることを実機確認した。
VoiceCord も中央の送信停止への切替を撤去し、再生アイコンのままにした。
既存の信頼済みクリック検査とタイルの再生処理を使い続ける。全停止は従来どおり。
試聴と音量設定はローカル音源の既存機能として維持。お気に入り機能の追加はしていない。
再生時だけ付く独自の緑枠も撤去した。
最新状態の型チェックとビルド成功。ui.js と map を Canary の配置先へ反映し、
ui.js の SHA-256 一致を確認して Discord ページを再読込した。
chime のクリック前後でポップアウトは 531x576 のまま。再生中 region と送信停止ボタンは 0 件。
検証後は全停止を押した。MCP はヘッドレスのまま。Stable は操作していない。

Playwright MCP の設定に --headless を追加したが、既存サーバーは旧引数のまま稼働していた。
MCP 側ブラウザを閉じて旧サーバーを停止した。以後の呼び出しは Transport closed。
MCP を再起動してから純正との比較を再開する。Discord Canary 自体は停止していない。

声の新しい測定値が config.json に保存されていることを確認した。
voiceRms 0.16249340542628019、voicePeak 0.9413475394248962、activeRatio 0.6266666666666667。
目標は -6.5 dB。受信側での音量バランスの確認はまだ行っていない。

UI 修正後に校正状態を再確認。master 2.0315884748503583、基準 -16.4 dBFS、
新しい声の測定値と目標 -6.5 dB が保存済みだった。
音源を再測定して 32 件が推奨値と一致することを確認した。
ステレオ bye-bye-soundbible とモノラル voice-message を順に再生し、受信側の評価待ち。
受信側の回答は「ちょうどいい」。代表音源の音量バランスを受け入れ、M4 完了。
速度・ピッチ・音割れについて個別の数値や詳細な回答はなく、今回の回答は音量評価として記録する。

続けてテーマ検証を開始。ライト切替自体は反映されたが、設定を開き直すと音声・ビデオ画面へ
戻るため自動操作の次のラジオ検出がタイムアウトした。オニキスの検証は未実施。
設定のテーマ画面へ明示的に移動し、描画を待ってからダークを再選択。
html の theme-dark / theme-darker を確認して元のダークへ復元し、設定を閉じた。
ライトでの VoiceCord 寸法・色の検証は完了扱いにしない。

その後テーマ操作を個別に実施して検証成功。
ライトは明るい背景と暗い文字、オニキスは暗い背景と明るい文字へ追従。
アニメーション完了後の寸法は両方 531x576。FAB は両方非表示。
オニキスは html の theme-midnight と一致。最後に theme-dark / theme-darker へ復元した。
VC 接続を維持したまま DM 一覧、uta_a の DM 本文、サーバー VC へ移動し、接ぎ木は 1 件。
VC 全画面のポップアウトは通常クリックで開けた。force click とキーボード回避は使っていない。
ポップアウト窓の #vc-root は 0 件。「Discordに戻る」で閉じた。
1 分の再挿入回数、VC 未参加時の表示、Vencord 共存、故障時 FAB の非遮蔽は未確認。

72 秒の定常観察で再挿入は 1 回のまま。これは初回挿入を含む累計で、観察中の増加は 0 回。
退出後は接ぎ木ボタン 0、可視 FAB 0、VoiceCord ポップアウト 0 を確認。
一般 VC に再入場してボタンと設定パネルが復帰。CSS 採取は未取得で 3 段目の予備探索を使用。
第 1 段の CSS 採取が VC 中でも未取得である点は調査残件とする。
続いて原因を修正済み。複数 runtime のキャッシュ選択、短縮 CSS キー、単独ラッパーに対応。
Canary への配置後に「取得済み（webpack）」「1 段目で発見」を確認。
関連 47 テスト・型チェック・ビルド成功。詳細は DEV-NOTES.md。
この定常観察は DOM が繰り返し削除される状況の上限・再試行検証とは区別する。
